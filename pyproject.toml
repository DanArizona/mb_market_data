[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "mb-market-data"
version = "0.1.0.dev0"
description = "Market-data acquisition, historical storage, and feature generation for MasterBot."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
